image: python:latest

stages:
    - build
    - test

pre-job:
    stage: .pre
    script:
        - echo "iam pre job"

build-job:
    stage: build
    script:
        - echo "Building..."
        - ls -l
test-db:
    stage: test
    script:
        - ls -l
        - test -f ./resources/data/data.db && echo "Test passed"
        - test -f $CI_PROJECT_DIR/resources/data/data.db && echo "Test db passed."
